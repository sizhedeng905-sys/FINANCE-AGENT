# Required build-time mode. Use mock only for local development and deterministic tests.
VITE_APP_DATA_MODE=mock
# Required API base URL. Same-origin paths such as /api are supported.
VITE_API_BASE_URL=http://127.0.0.1:3001/api
VITE_API_TIMEOUT_MS=15000
